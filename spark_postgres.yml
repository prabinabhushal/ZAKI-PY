SPARK:
  EXECUTOR:
    CORES: 8           #lscpu | grep "^CPU(s):"
    INSTANCES: 1
    MEMORY: 1g         #grep -i "executor" ~/spark/conf/*
  DRIVER:
    MEMORY: 4g         #grep -iR "spark.driver.memory" .

  


POSTGRES:
  port : 5432
  host : 'localhost'
  dbname : 'postgres'
  user : 'postgres'
  password : '123'
 

